<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User List</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div
      class="users p-6 bg-white shadow-lg rounded-lg w-full flex flex-wrap justify-center"
    >
      <% if(users.length > 0){ %> <% users.forEach((e) => { %>
      <div
        class="user p-4 bg-gray-50 w-1/3 rounded-lg border border-gray-200 text-center m-4"
      >
        <img class="w-full rounded-lg" src="<%= e.image %>" alt="" />
        <h1 class="text-xl font-semibold text-gray-800 mb-2"><%= e.name %></h1>
        <h2 class="text-lg text-gray-600 mb-2"><%= e.email %></h2>
        <div class="flex justify-between mx-5">
          <a
            href="/delete/<%= e._id %>"
            class="text-white bg-red-500 hover:bg-red-600 px-4 py-2 rounded-lg shadow"
            >Delete User</a
          >
          <a
            href="/update/<%= e._id %>"
            class="text-white bg-blue-500 hover:bg-blue-600 px-4 py-2 rounded-lg shadow"
            >Update User</a
          >
        </div>
      </div>
      <% }) %> <% }else{ %>
         <h1 class="text-5xl">Nothing to see here</h1>
        <% } %>
    </div>
  </body>
</html>
